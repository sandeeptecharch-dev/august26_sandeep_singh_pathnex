Day 13, 14 August 2026
1. Amazon EKS Basics
	What is EKS?
	EKS Cluster
	EKS Control Plane
	Worker Node
	Node Group
	EKS architecture
	Relationship:
		EKS Cluster → Node Group → Nodes → Pods
2. Kubernetes Node
	What is a Node?
	EC2 instance as a Worker Node
	Components running on a Node:
		Kubelet
		Kube-proxy
		Container Runtime
		Application Pods
3. EKS Node Group
	What is a Node Group?
	Why Node Groups are used
	Multiple Node Groups
		Frontend Node Group
		Backend Node Group
	Node Group configuration:
		EC2 instance type
		Minimum nodes
		Desired nodes
		Maximum nodes
		Kubernetes version
		AMI
		Labels
		IAM Role
4. kubectl
	What is kubectl?
	Connecting kubectl to EKS
	Basic commands:
		kubectl get nodes
		kubectl get pods
		kubectl get deployments
		kubectl get services
		kubectl get all
	Resource management:
	kubectl apply -f
	kubectl delete
	kubectl describe
	kubectl logs

5. Kubernetes Context
	What is a Kubernetes Context?
	Cluster
	User
	Namespace
	.kube/config
	Multiple cluster configuration
	Switching between contexts

6. kubectx
	What is kubectx?
	Switching between Kubernetes clusters
	Example:
		Production
		Staging
		Development
		kubectx prod

7. Kubernetes Namespaces
	What is a Namespace?
	Why namespaces are used
	Namespace as logical separation of resources
	kubectl get namespaces
	kubens
	Switching namespaces
	kubens default
8. Default Kubernetes Namespaces
	default
	kube-system
	kube-public
	kube-node-lease

9. AWS Authentication & EKS Connection
	aws configure
	AWS Access Key
	AWS Secret Access Key
	IAM User
	IAM permissions
	aws eks update-kubeconfig
	EKS kubeconfig
	Adding EKS cluster context

10. kubectl Aliases
	alias kgn="kubectl get nodes"
	alias kgp="kubectl get pods"
	kgp -A
	kgp -n default

11. Kubernetes Pods
	What is a Pod?
	Pod and Node relationship
	Pods inside namespaces
	Getting pods:
		kubectl get pods
		kubectl get pods -A
		kubectl get pods -n default

12. Kubernetes Services
	What is a Kubernetes Service?
	Service as a stable way to access Pods
	Service selects a group of Pods
	Logical grouping of Pods
	Pod IP vs Service IP
	Service Types:
		ClusterIP
		NodePort
		LoadBalancer
		ExternalName
